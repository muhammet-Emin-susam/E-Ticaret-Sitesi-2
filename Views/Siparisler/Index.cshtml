@model List<E_TicaretOtomasyonu.Models.UserOrderModel>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<section class="breadcrumb-section set-bg" data-setbg="~/Content/img/shopping-cart-banner.jpg">
        <div class="container">
            <div class="row">
                <div class="col-xl-12 text-center">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Sipariş İçeriği</th>
                                <th scope="col">Toplam Tutar</th>
                                <th scope="col">Sipariş Veriliş Tarihi</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">@item.OrderID</th>
                                    <td>@item.OrderName</td>
                                    <td>@item.TotalPrice.ToString("C")</td>
                                    <td>@item.AddedDate</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </section>*@
<section class="product spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 col-md-12">
                <div id="Hepsi" class="Products">
                    <div class="Cards-Row">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Sipariş İçeriği</th>
                                    <th scope="col">Toplam Tutar</th>
                                    <th scope="col">Sipariş Veriliş Tarihi</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <th scope="row">@item.OrderID</th>
                                        <td>@item.OrderName</td>
                                        <td>@item.TotalPrice.ToString("C")</td>
                                        <td>@item.AddedDate</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <!-- Featured Section End -->
            </div>
        </div>
    </div>
</section>
@*<table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Sipariş İçeriği</th>
                <th scope="col">Toplam Tutar</th>
                <th scope="col">Sipariş Veriliş Tarihi</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item.OrderID</th>
                    <td>@item.OrderName</td>
                    <td>@item.TotalPrice.ToString("C")</td>
                    <td>@item.AddedDate</td>
                </tr>
            }
        </tbody>
    </table>*@